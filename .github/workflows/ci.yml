name: CI
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
jobs:
  build-translator:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: openai-translate
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: openai-translate/package-lock.json
      - run: npm ci
      - run: npm run build
